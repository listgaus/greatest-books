import { CapitalizeTitlePipe } from './capitalize-title.pipe';

describe('CapitalizeTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
